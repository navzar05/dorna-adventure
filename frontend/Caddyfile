# global options
{
    admin off # there's no need for the admin api in railway's environment
    persist_config off # storage isn't persistent anyway
    auto_https off # railway handles https for us, this would cause issues if left enabled
    # runtime logs
    log {
        format json # set runtime log format to json mode
    }
    # server options
    servers {
        trusted_proxies static private_ranges 100.0.0.0/8 # trust railway's proxy
    }
}

# site block, listens on the $PORT environment variable, automatically assigned by railway
:3000 {
    # access logs
    log {
        format json # set access log format to json mode
    }

    # health check for railway
    rewrite /health /*

    # FIX 1: Pune calea absolută, nu relativă
    root * /app/dist

    # FIX 2 (Opțional dar recomandat): Health check-ul tău e riscant.
    # Dacă rescrii /health către /*, încarci tot React-ul pentru un simplu check.
    # Folosește asta pentru un răspuns instant:
    handle /health {
        respond "OK" 200
    }

    encode gzip
    file_server
    try_files {path} /index.html
}